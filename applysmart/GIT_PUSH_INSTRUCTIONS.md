# ApplySmart — Git push (run locally)

The repo was created from the SaaS Starter Kit ZIP (clone failed in environment). Run these in your terminal from the `applysmart` folder after creating **github.com/visuxlize/applysmart**:

```bash
cd /Users/dre/applysmart
git init
git add .
git commit -m "feat: phase 0 — waitlist landing page + admin dashboard"
git remote add origin https://github.com/visuxlize/applysmart.git
git branch -M main
git push -u origin main
```

Then delete this file: `rm GIT_PUSH_INSTRUCTIONS.md` and commit if you like.
